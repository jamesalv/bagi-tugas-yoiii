#include <stdio.h>

int main()
{
    printf("\n");

    // Array
    // Struktur data yang bisa menyimpan banyak nilai dengan tipe data yang sama
    // <tipe_data> <nama_variabel> [<size>]

    // int nilai[6];
    // for (int i = 0; i < 6; i++)
    // {
    //     scanf("%d", &nilai[i]);
    // }

    // for (int i = 0; i < 6; i++)
    // {
    //     printf("Nilai ke - %d adalah %d\n", i+1, nilai[i]);
    // }
    
    // char hewan[5][100] = {"jerapah", "gajah", "kucing", "anjing", "zebra"};
    // // // printf("%s", hewan[3]);
    // // printf("%c", hewan[2][5]);

    // for (int i = 0; i < 5; i++)
    // {
    //     printf("Hewan ke - %d adalah %s\n", i+1, hewan[i]);
    // }

    // Pointer
    // <tipe_data> *<nama_pointer>
    // * --> dereferencing
    // & --> address

    // int n = 5;
    // int *ptr = &n;

    // printf("Nilai n awal = %d\n", n);
    // *ptr = 6;
    // printf("Nilai n akhir = %d\n", n);
    // printf("Pointer = %d", *ptr);

    // int arr[5] = {3, 1, 3, 4, 5};
    // int *ptr;

    // ptr = arr; // menuju ke alamat indeks pertama array (index 0)
    // for (int i = 0; i < 5; i++)
    // {
    //     printf("Pointer = %d\n", *(ptr+i));
    // }

    printf("\n");
    return 0;
}